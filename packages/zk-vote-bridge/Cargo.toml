[package]
name = "zk-vote-bridge"
version = "0.1.0"
edition = "2021"
description = "Bridges vote-sdk Halo2 circuits into the CosmWasm VM's proof_instance_verify API via CircuitFooter serialization"

[lib]

[dependencies]
shielded-vote-circuits = { path = "../../../vote-sdk/circuits" }
zk-cosmwasm = { workspace = true }
halo2_proofs = { path = "../../../zcash/halo2/halo2_proofs" }
sha2 = "0.10"
pasta_curves = "0.5"
group = "0.13"